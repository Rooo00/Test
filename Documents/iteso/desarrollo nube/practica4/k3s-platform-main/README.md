# k3s-platform

Este repositorio funciona como base para hacer un deployment de k3s de dos nodos
en AWS academy. En `cluster` están las instrucciones para crear el cluster y en
º`deployment` se encuentra un manifest de ejemplo.
